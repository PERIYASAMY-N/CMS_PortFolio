# =====================================================
# PRODUCTION CONFIGURATION FOR RENDER
# =====================================================
spring.application.name=portfolio-cms

# =====================================================
# DATABASE CONFIGURATION (From Render Environment)
# =====================================================
# Database URL is configured in DatabaseConfig.java to handle postgres:// and postgresql:// URL formats
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-test-query=SELECT 1

# =====================================================
# JPA / HIBERNATE
# =====================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# =====================================================
# SERVER
# =====================================================
server.port=${PORT:8080}
server.servlet.context-path=/

# =====================================================
# JWT CONFIGURATION
# =====================================================
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# =====================================================
# FILE UPLOAD
# =====================================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
app.upload.dir=/opt/render/project/src/uploads/

# =====================================================
# CLOUDINARY (Image Storage)
# =====================================================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# =====================================================
# ADMIN CREDENTIALS
# =====================================================
app.admin.username=${ADMIN_USERNAME:admin}
app.admin.password=${ADMIN_PASSWORD}
app.admin.email=${ADMIN_EMAIL:admin@portfolio.com}

# =====================================================
# CORS (Allow Vercel Frontend)
# =====================================================
app.cors.allowed-origins=${FRONTEND_URL:https://cms-port-folio-x4t6.vercel.app}

# =====================================================
# ACTUATOR (for health checks)
# =====================================================
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
